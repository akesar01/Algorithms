def check(a):
    a=list(a)
    a=list(map(int,a))
    for i in a:
        if(i==3):
            return 0
    return 1

def func(a):
    for i in range(1,a+1):
        if(check(str(i))):
            print(i)
        continue




a=int(input("Enter the number n: "))
func(a)
